# NOVA MARKET — تقرير حالة المشروع

**تاريخ التدقيق:** 2026-08-07  
**نطاق التدقيق:** فحص الملفات الموجودة فعليًا، تشغيل lint وbuild محليًا، والتحقق من نسخة VPS الحالية.  
**آخر commit مفحوص:** 5c79aab (chore: configure market proxy on port 3005)

> هذا التقرير توثيقي فقط. لم تتم إضافة قاعدة بيانات أو تعديل الاعتماديات أو تغيير كود التطبيق أثناء إعداد هذا التقرير. المشروع حاليًا Frontend تجريبي يعتمد على بيانات Mock وذاكرة المتصفح.

## 1. الملخص التنفيذي

NOVA MARKET هو واجهة متجر إلكتروني عربية باتجاه RTL، مع لوحة إدارة داخلية بنفس الهوية البصرية. الهدف الحالي هو تقديم تجربة عرض مقنعة للعميل قبل بدء التنفيذ التجاري الكامل.

المشروع مبني كواجهة أمامية فقط:

- لا توجد قاعدة بيانات.
- لا يوجد Backend أو API حقيقي.
- لا توجد عمليات دفع أو طلبات حقيقية.
- البيانات الموجودة داخل التطبيق ثابتة أو محفوظة مؤقتًا في حالة React أو localStorage.

### ما تم بناؤه ويعمل حاليًا

- واجهة متجر رئيسية بتصميم داكن Glassmorphism مع ألوان Neon.
- خلفية Canvas متحركة بنقاط وجسيمات، مع Orbs ضوئية.
- عرض منتجات مع السعر الحالي وسعر المقارنة والمصدر الأوروبي.
- أقسام للمنتجات وروابط تصفح حسب القسم.
- صفحات تصنيف، تفاصيل منتج، السلة، المفضلة، تسجيل الدخول، الحساب، وإتمام طلب تجريبي.
- إضافة المنتجات إلى السلة وإزالتها، وإضافة المنتجات إلى المفضلة.
- بحث مساعد ومقارنة بين حتى ثلاثة منتجات.
- تبديل العملة بين الدولار الأمريكي USD والليرة السورية SYP واليورو EUR، مع أسعار تحويل تجريبية.
- شريط عروض، المنتجات التي شوهدت مؤخرًا، ومزايا الشحن والإرجاع والتواصل.
- لوحة إدارة بتسجيل دخول تجريبي.
- إدارة تجريبية للمنتجات والأقسام والمخزون والطلبات والعملاء والإعدادات.
- صفحة عروض تدعم واجهة تخفيضات، باقات، كوبونات، وخصم حسب الكمية.
- صفحة تقارير مقسمة إلى مبيعات وعروض وأكثر مبيعًا وأقل مبيعًا ومخزون وحركة بيع.
- إعداد تشغيل على VPS باستخدام Next.js وPM2 وNginx على المنفذ الداخلي 3005.

### السياق التجاري المقصود

الفكرة التجارية هي إعادة بيع مخزون فائض أو بطيء الحركة من متاجر أوروبية كبيرة، مع تقديم المنتجات للزبون على أنها منتجات جديدة وبعلبتها. يظهر مصدر المنتج أو العلامة الأوروبية في البطاقات وصفحة التفاصيل كعنصر ثقة وشفافية، وليس على أنها منتجات مستعملة أو مستعملة جزئيًا.

## 2. التقنية الفعلية

### Frameworks والمكتبات

القيم التالية مأخوذة من package.json، مع ذكر النسخ المثبتة حاليًا في node_modules عند توفرها:

| التقنية | المعلن في package.json | النسخة المثبتة التي تم فحصها |
|---|---:|---:|
| Next.js | 16.3.0 | 16.3.0 |
| React | 19.2.8 | 19.2.8 |
| React DOM | 19.2.8 | 19.2.8 |
| TypeScript | ^5 | 5.9.3 |
| Framer Motion | ^13.0.0 | 13.0.0 |
| lucide-react | ^1.30.0 | 1.30.0 |
| Tailwind CSS | ^4 | 4.3.3 |
| @tailwindcss/postcss | ^4 | 4.3.3 |
| ESLint | ^9 | 9.39.5 |
| eslint-config-next | 16.3.0 | 16.3.0 |
| @types/node | ^20 | 20.19.43 |
| @types/react | ^19 | 19.2.18 |
| @types/react-dom | ^19 | 19.2.4 |

### ملاحظات تقنية مهمة

- يستخدم المشروع Next.js App Router داخل src/app.
- يستخدم TypeScript مع مكونات React Client Components.
- يستخدم Tailwind CSS 4 عبر @tailwindcss/postcss، إلى جانب ملف CSS مخصص كبير في src/app/globals.css.
- يستخدم Framer Motion لحركات ظهور بطاقات المنتجات والتنبيهات.
- يستخدم lucide-react للأيقونات.
- لا توجد مكتبة State خارجية مثل Zustand أو Redux.
- لا يوجد React Context حاليًا؛ الحالة تتم إدارتها داخل المكونات باستخدام useState وuseEffect.
- لا توجد حزمة next-pwa أو Workbox أو أي حزمة Service Worker ضمن الاعتماديات.

### الخطوط

يتم تحميل الخطين من خلال next/font/google في src/app/layout.tsx:

- Tajawal: النصوص العربية والأوصاف والحقول.
- Orbitron: الشعار والعناوين والأسعار والأرقام والإحصاءات.

يتم حقن الخطين كمتغيري CSS باسمَي --font-tajawal و--font-orbitron، مع وضع lang="ar" وdir="rtl" على عنصر html.

## 3. هيكل المشروع الفعلي

الشجرة التالية تلخص الملفات والمجلدات المهمة الموجودة فعليًا، وليس الشجرة المقترحة في ملف المهمة:

~~~text
market/
├─ src/
│  └─ app/
│     ├─ layout.tsx
│     ├─ page.tsx
│     ├─ globals.css
│     ├─ favicon.ico
│     ├─ MarketApp.tsx
│     ├─ CustomerCategoryBar.tsx
│     ├─ CustomerEnhancements.tsx
│     ├─ AdminPortal.tsx
│     ├─ AdminCategoriesPage.tsx
│     ├─ AdminProductCurrencyForm.tsx
│     ├─ AdminPromotionsPage.tsx
│     ├─ AdminReportsPage.tsx
│     ├─ [...slug]/page.tsx
│     └─ admin/
│        ├─ page.tsx
│        ├─ [...slug]/page.tsx
│        ├─ categories/page.tsx
│        ├─ products/new/page.tsx
│        ├─ products/[id]/edit/page.tsx
│        ├─ promotions/page.tsx
│        └─ reports/page.tsx
├─ public/
│  ├─ manifest.webmanifest
│  ├─ file.svg
│  ├─ globe.svg
│  ├─ next.svg
│  ├─ vercel.svg
│  └─ window.svg
├─ deploy/
│  └─ market-nginx.conf
├─ package.json
├─ package-lock.json
├─ next.config.ts
├─ postcss.config.mjs
├─ tsconfig.json
├─ eslint.config.mjs
├─ .env.local.example
├─ .gitignore
├─ README.md
├─ AGENTS.md
├─ CLAUDE.md
└─ تاسك
~~~

### وظيفة المجلدات الأساسية

| المسار | الواقع الحالي |
|---|---|
| src/app/ | يحتوي كل الصفحات والمكونات تقريبًا، بما في ذلك واجهة الزبون ولوحة المدير والخدمات البصرية. |
| src/app/admin/ | يحتوي أغلفة مسارات لوحة الإدارة. بعض الصفحات تستدعي AdminPortal، وبعضها يستدعي مكونات إدارة متخصصة. |
| src/app/globals.css | ملف التصميم الرئيسي: الألوان، Glassmorphism، RTL، الشبكات، الاستجابة، الشريط السفلي للموبايل، والجسيمات البصرية المساعدة. |
| public/ | ملفات عامة ثابتة. يحتوي Manifest وملفات SVG الافتراضية، ولا يحتوي حاليًا أيقونات PWA بأحجام 192/512 أو Service Worker. |
| deploy/ | إعداد Nginx المستخدم لتوجيه الطلبات إلى 127.0.0.1:3005. |
| src/components/ | غير موجود حاليًا. المكونات موجودة مباشرة في src/app/. |
| src/context/ | غير موجود حاليًا. لا يوجد React Context. |
| src/data/ | غير موجود حاليًا. بيانات Mock معرفة داخل ملفات المكونات. |
| src/lib/api/ | غير موجود حاليًا. لا توجد طبقة Data Access أو API wrapper. |
| src/lib/hooks/ | غير موجود حاليًا. Hooks معرفة داخل المكونات نفسها. |
| src/types/ | غير موجود حاليًا. الأنواع TypeScript محلية داخل الملفات. |
| src/styles/ | غير موجود حاليًا. ملف التصميم الفعلي هو src/app/globals.css. |

## 4. جرد الشاشات والمسارات

### 4.1 واجهة الزبون

> المسارات العامة يتم التعامل معها غالبًا عبر src/app/[...slug]/page.tsx، ثم يقرر MarketApp ما يعرضه اعتمادًا على usePathname().

| الشاشة | المسار | ما تعرضه | الحالة |
|---|---|---|---|
| الرئيسية | / | Hero، المنتجات الجديدة، الأكثر رواجًا، الأقسام، العرض الدعائي، والإحصاءات. | تفاعلية تجريبيًا |
| الأقسام | /category/[slug] | قائمة المنتجات حسب القسم أو العروض أو المنتجات الجديدة. | تفاعلية جزئيًا؛ البحث والفرز يعملان، زر التصفية لا يطبق فلاتر فعلية |
| الإلكترونيات | /category/electronics | المنتجات الموجودة في الكتالوج، مع أن الكود يعامل الإلكترونيات كقائمة عامة. | تفاعلية تجريبيًا |
| المنزل/الأزياء/الأطفال/العناية/الرياضة | /category/home, /category/fashion, /category/kids, /category/care, /category/sport | منتجات القسم المطابق. | تفاعلية تجريبيًا |
| العروض | /category/offers | المنتجات التي لديها سعر قديم أعلى من الحالي. | عرض Mock تفاعلي |
| الجديد | /category/new | أول أربعة منتجات في القائمة الحالية. | عرض Mock |
| تفاصيل المنتج | /product/[slug] | صورة المنتج، الوصف، المصدر الأوروبي، المواصفات، السعر، المخزون، المفضلة، ومنتجات مشابهة. | تفاعلية جزئيًا |
| البحث | /search | لا توجد صفحة بحث مستقلة؛ المسار غير المعروف يعود إلى الصفحة الرئيسية. البحث الفعلي موجود في نافذة Search Assistant العائمة. | نافذة بحث تفاعلية، المسار نفسه ليس شاشة مستقلة |
| السلة | /cart | المنتجات المختارة، الحسابات التجريبية، الخصم الثابت 10%، والانتقال للدفع. | تفاعلية داخل الجلسة |
| المفضلة | /wishlist | المنتجات المحفوظة في حالة React. | تفاعلية داخل الجلسة، غير محفوظة دائمًا |
| إتمام الطلب | /checkout | خطوات العنوان والشحن والدفع والمراجعة، ثم تأكيد وهمي برقم NV-1052. | تفاعلية تجريبيًا؛ لا يوجد دفع أو إنشاء طلب حقيقي |
| تسجيل الدخول | /login | تسجيل دخول زبون أو انتقال تجريبي حسب البريد المدخل. | محاكاة فقط |
| إنشاء حساب | /register | نموذج إنشاء حساب تجريبي. | محاكاة فقط؛ لا ينشئ مستخدمًا دائمًا |
| الحساب | /account | إحصاءات ثابتة وروابط الطلبات والعناوين والإرجاع. | عرض Mock |
| طلباتي | /account/orders | أول ثلاثة طلبات من مصفوفة الطلبات الوهمية. | عرض Mock |
| العناوين والإرجاع وتفاصيل الطلب | /account/addresses, /account/returns, /account/orders/[id] | لا توجد مكونات مستقلة لهذه الشاشات؛ مسار /account العام يعرض شاشة الحساب العامة. | غير منفذة كشاشات مستقلة |

### 4.2 لوحة الإدارة

الدخول الإداري الفعلي يتم من /admin. بيانات التجربة الموجودة في الكود هي:

- البريد: admin@nova.store
- كلمة المرور: admin123

يتم وضع القيمة nova-admin-auth=1 في localStorage بعد النجاح. هذا ليس نظام صلاحيات حقيقيًا.

| الشاشة | المسار | ما تعرضه | الحالة |
|---|---|---|---|
| تسجيل دخول المدير / لوحة المعلومات | /admin | شاشة الدخول عند عدم وجود جلسة، ثم KPIs ومخطط مبيعات وآخر بيانات عند الدخول. | تفاعلية تجريبيًا |
| المنتجات | /admin/products | جدول المنتجات مع بحث، حذف محلي، وزر تعديل. | تفاعلية جزئيًا؛ البيانات لا تحفظ بعد إعادة التحميل |
| إضافة منتج | /admin/products/new | نموذج الاسم وSKU والقسم والمصدر وثلاثة أسعار USD/SYP/EUR والكمية والوصف. | تفاعلية تجريبيًا؛ زر الحفظ لا يضيف إلى مخزن دائم |
| تعديل منتج | /admin/products/[id]/edit | نفس نموذج العملات مع بيانات مثال عند وجود ID. | تفاعلية تجريبيًا؛ لا يوجد تحديث دائم |
| الأقسام | /admin/categories | قائمة أقسام مع إضافة وتعديل وحذف محلي. | تفاعلية داخل الجلسة فقط |
| الطلبات | /admin/orders | جدول الطلبات، أزرار بحث/تصفية شكلية، وتصدير تجريبي. | عرض Mock مع انتقال للتفاصيل |
| تفاصيل الطلب | /admin/orders/[id] | ملخص عميل وخطوات حالة الطلب. | تفاعلية داخل الجلسة فقط |
| المخزون | /admin/inventory | كميات ومتاح وحد تنبيه، مع نموذج تعديل كمية وسبب. | تفاعلية تجريبيًا؛ لا تغير بيانات المنتجات فعليًا |
| العروض والتخفيضات | /admin/promotions | تخفيض منتج، باقة، كوبون، وخصم كمية، مع اختيار منتج واحد أو عدة منتجات. | تفاعلية داخل الجلسة فقط |
| العملاء | /admin/customers | جدول عملاء وإجمالي إنفاق وعدد طلبات. | عرض Mock |
| التقارير | /admin/reports | ستة تقارير: المبيعات، العروض، الأكثر مبيعًا، الأقل مبيعًا، المخزون، وحركة البيع. | تفاعلية في التبديل والعرض؛ البيانات ثابتة والتصدير Mock |
| الإعدادات | /admin/settings | اسم المتجر والبريد والتواصل والشحن والوصف. | نموذج Mock؛ زر الحفظ لا يكتب إلى Backend |
| الأدوار | /admin/roles | لا توجد صفحة مستقلة فعلية؛ المسار العام غير المعروف يعود إلى لوحة المعلومات. | غير منفذة |
| المرتجعات | /admin/returns | لا توجد صفحة مستقلة فعلية؛ المسار العام غير المعروف يعود إلى لوحة المعلومات. | غير منفذة |

## 5. طريقة عمل البيانات — Mock وبدون قاعدة بيانات

### مكان البيانات

لا يوجد في المشروع مجلد src/data/* رغم أن ملف المهمة الأصلي اقترح وجوده. البيانات موجودة فعليًا داخل المكونات التالية:

- src/app/MarketApp.tsx: المنتجات، الأقسام، الطلبات، أسعار المنتجات، المواصفات، والمحتوى الرئيسي.
- src/app/AdminPortal.tsx: نسخ مستقلة من منتجات الإدارة وطلبات الإدارة، مع بيانات العملاء والإعدادات والمخزون.
- src/app/AdminCategoriesPage.tsx: قائمة الأقسام الأولية.
- src/app/AdminPromotionsPage.tsx: كتالوج منتجات العروض والحملات الأولية.
- src/app/AdminReportsPage.tsx: مصفوفات المبيعات والعروض والأكثر/الأقل مبيعًا والمخزون وحركة البيع.
- src/app/CustomerEnhancements.tsx: قائمة منتجات مصغرة، العروض، والمنتجات المشاهدة مؤخرًا.

هذا يعني وجود أكثر من نسخة من بعض البيانات، وليس مصدرًا موحدًا للمنتجات أو الطلبات.

### طبقة الوصول للبيانات

لا يوجد حاليًا أي ملف في src/lib/api/*، ولا توجد دوال Async مخصصة لقراءة أو كتابة البيانات، ولا توجد استدعاءات fetch() أو API routes أو Server Actions للبيانات.

الآلية الفعلية هي:

- useState لحالة السلة والمفضلة والنماذج والقوائم المحلية.
- useMemo للبحث والفرز والحسابات داخل المكونات.
- localStorage لثلاثة أشياء رئيسية: جلسة المدير، العملة المختارة، والمنتجات المشاهدة مؤخرًا.
- إعادة تحميل الصفحة عند تبديل العملة لتحديث الأسعار المعروضة.

لذلك، عبارة “طبقة Data Access ترجع Promises من Mock data” موجودة في مواصفات المهمة الأصلية، لكنها غير موجودة في الكود الحالي. لا يمكن توصيل Backend لاحقًا دون تعديل إضافي لاستخراج البيانات إلى وحدات API/Types مشتركة، مع بقاء واجهة المستخدم قابلة لإعادة الاستخدام.

### ما هو منطق حقيقي داخل الواجهة؟

- إضافة منتج إلى السلة وإزالته من السلة.
- تغيير المفضلة وإظهار عددها.
- البحث داخل قائمة المنتجات في بعض الشاشات.
- فرز المنتجات حسب السعر أو التقييم أو الترتيب الحالي.
- حساب المجموع والخصم والشحن المجاني داخل شاشة السلة.
- الانتقال بين خطوات الدفع التجريبي.
- التحقق الشكلي من الحقول المطلوبة.
- تسجيل الدخول الإداري التجريبي والتحويل إلى لوحة الإدارة.
- حراسة بعض صفحات الإدارة باستخدام nova-admin-auth في localStorage.
- اختيار منتجات متعددة للعروض والباقات.
- تبديل العملة وفق معاملات ثابتة تجريبية: 1 USD = 13000 SYP و1 USD = 0.92 EUR.
- البحث المساعد، المقارنة حتى ثلاثة منتجات، والمنتجات المشاهدة مؤخرًا.

### ما هو عرض فقط أو Mock-only؟

- أرقام المبيعات وKPIs والمخططات والتقارير.
- العملاء والطلبات والمخزون والحملات الأولية.
- حفظ المنتج أو القسم أو العرض؛ الحفظ يغير حالة المكون فقط.
- تعديل حالة الطلب؛ التغيير يختفي بعد إعادة التحميل.
- الدفع، إنشاء الطلب، رقم الطلب، والشحن.
- الكوبونات؛ توجد واجهة إدارة لها، لكن لا يوجد تحقق فعلي منها داخل checkout.
- زر التصدير؛ يعرض رسالة أو لا ينفذ ملف تصدير حقيقي.
- الإعدادات؛ الحقول لا تحفظ في قاعدة أو ملف.
- المصادقة؛ البريد وكلمة المرور مكتوبان داخل Frontend ويمكن رؤيتهما.

### كيف يتم توصيل Backend مستقبلًا؟

الطريقة الصحيحة في المرحلة التالية هي استخراج الأنواع والبيانات من المكونات إلى src/types وsrc/data، ثم إنشاء وظائف Async في src/lib/api مثل getProducts وgetCategories وcreateOrder وupdateInventory. بعد ذلك تستبدل هذه الوظائف مصادر Mock بمكالمات API، بينما تبقى مكونات العرض وأسماء الوظائف والعقود النوعية ثابتة قدر الإمكان. هذا مسار مستقبلي؛ لم يتم تنفيذه ضمن المشروع الحالي.

## 6. التشغيل المحلي

### المتطلبات التي تم التحقق منها

- Node.js: تم تشغيل المشروع محليًا على v22.12.0.
- npm: تم تشغيله محليًا على 11.16.0.
- يفضل استخدام Node 22 أو أحدث LTS متوافق.
- لا يحتاج المشروع إلى قاعدة بيانات أو خدمة Backend كي يعمل في وضع العرض الحالي.

### التثبيت والتشغيل

من جذر المشروع:

~~~bash
npm install
npm run dev
~~~

يفتح المشروع افتراضيًا على:

~~~text
http://localhost:3000
~~~

### فحوص الجودة والبناء

~~~bash
npm run lint
npm run build
npm start
~~~

npm start يشغل نسخة الإنتاج افتراضيًا على المنفذ 3000 ما لم يتم تمرير منفذ مختلف. لا يوجد Script باسم preview في package.json؛ المعاينة الإنتاجية المقصودة هي npm run build ثم npm start.

آخر فحص محلي أثناء إعداد التقرير:

- npm run lint: نجح.
- npm run build: نجح، وتم توليد صفحات Next.js دون أخطاء TypeScript.

### ملف البيئة

يوجد .env.local.example ويحتوي:

~~~env
DATABASE_PASSWORD=12345678
~~~

هذه القيمة Placeholder مستقبلية فقط. لا يوجد Backend حاليًا، ولا يتم استخدام هذا المتغير في التطبيق الحالي، ولا توجد قاعدة بيانات متصلة به.

## 7. النشر السحابي الحالي

### البنية الفعلية

- الاستضافة: VPS بعنوان IP 65.21.136.217.
- نظام التشغيل المفحوص: Ubuntu 24.04.4 LTS.
- مسار المشروع: /home/ubuntu/market.
- Node.js على السحابة: v22.22.3.
- npm على السحابة: 11.17.0.
- مدير العملية: PM2، واسم العملية market.
- أمر التشغيل: Next production server على 0.0.0.0:3005.
- Reverse proxy: Nginx على HTTP port 80، يوجه إلى 127.0.0.1:3005.
- اسم Nginx الحالي: 65.21.136.217 وmarket.65.21.136.217.sslip.io.
- لا يوجد دومين رسمي للعميل حتى الآن.
- لا يوجد إعداد HTTPS/SSL حاليًا في إعداد Nginx المفحوص.

### ما تم التحقق منه على السحابة

- PM2 حالته online.
- التطبيق يستمع على 0.0.0.0:3005.
- طلب محلي من الخادم إلى Next على 127.0.0.1:3005 أعاد HTTP 200.
- طلب محلي عبر Nginx مع Host الخاص بـ Market أعاد HTTP 200.
- نسخة HTML المباشرة ونسخة Nginx أعادتا نفس حجم الصفحة في آخر تحقق.

أما الوصول العام من بيئة التدقيق إلى http://65.21.136.217:3005/ فلم يثبت نجاح HTTP؛ فحص TCP ظهر ناجحًا، لكن طلب HTTP أعاد reset/empty reply. لذلك لا يُسجل الوصول العام المباشر كاختبار ناجح إلى أن تفعيل المنفذ من شركة VPS ويُختبر من جهاز خارجي.

### خطوات النشر الفعلية

~~~bash
# محليًا
git push origin main

# عبر SSH إلى VPS على المنفذ 2727
cd /home/ubuntu/market
git fetch origin main
git reset --hard origin/main
npm ci --no-audit --no-fund
npm run build
pm2 restart market --update-env
pm2 save
~~~

ثم يتم التحقق من:

~~~bash
pm2 describe market
ss -ltnp | grep ':3005'
curl -I -H 'Host: market.65.21.136.217.sslip.io' http://127.0.0.1/
~~~

### خطوات إعادة النشر والتحديث مستقبلًا

بعد كل تعديل معتمد:

1. فحص المشروع محليًا وتشغيل npm run lint وnpm run build.
2. عمل Commit وPush إلى origin/main في GitHub.
3. الاتصال بالسحابة عبر SSH port 2727.
4. سحب origin/main داخل /home/ubuntu/market.
5. تشغيل npm ci ثم npm run build.
6. إعادة تشغيل PM2 للعملية market على 3005 ثم تنفيذ pm2 save.
7. اختبار Nginx والتأكد من HTTP 200.

لا ينبغي لهذا المسار أن يعدل UFW أو يوقف المشاريع الأخرى الموجودة على VPS. فتح الوصول العام للمنفذ 3005 يظل مسؤولية إعدادات شركة VPS/الشبكة الخارجية.

## 8. حالة PWA — التحقق من السحابة

### النتيجة المختصرة

حالة PWA الحالية **غير مكتملة وغير قابلة للتأكيد كميزة إنتاجية**. السبب ليس في Manifest وحده، بل في غياب HTTPS وغياب Service Worker فعلي وأيقونات PWA كاملة.

| الاختبار | النتيجة الفعلية |
|---|---|
| الوصول عبر HTTPS | غير متاح حاليًا؛ إعداد Nginx المفحوص يستمع على HTTP port 80 فقط ولا يحتوي SSL/443. |
| Manifest | موجود في public/manifest.webmanifest، ويعيد Nginx المحلي HTTP 200 بنوع application/manifest+json. |
| الاسم واللغة والاتجاه | موجودة: lang: ar وdir: rtl، مع display: standalone. |
| theme/background color | موجودة والقيمة #050510. |
| الأيقونات | Manifest يشير إلى /next.svg فقط بحجم any، ولا توجد مجموعة أيقونات 192/256/384/512 أو maskable مستقلة. |
| Service Worker | غير موجود. لا يوجد sw.js أو service-worker.js حقيقي ولا يوجد تسجيل navigator.serviceWorker. المساران يعيدان صفحة HTML من fallback بدل JavaScript Service Worker. |
| Android install | لم يثبت كتطبيق PWA إنتاجي بسبب عدم وجود HTTPS وService Worker؛ لم يسجل كاختبار ناجح. |
| iOS Add to Home Screen | لم يتم تأكيده على Safari؛ HTTPS وملفات iOS/PWA المطلوبة غير مجهزة كمسار إنتاجي. |
| Desktop install | لم يتم تأكيده كتثبيت PWA؛ لا توجد بيئة HTTPS وService Worker كاملة. |

بالتالي: يوجد Manifest شكلي، لكن المشروع ليس PWA مكتملًا في النسخة المنشورة حاليًا. كما أن الرابط المؤقت لا يمكن اعتباره رابطًا آمنًا لتجربة تثبيت PWA حتى يتم ربط دومين رسمي وإضافة HTTPS.

## 9. ما يعمل مقابل ما هو Mock فقط

### يعمل فعليًا داخل الواجهة

- التنقل بين المسارات الأساسية.
- التصميم RTL والاستجابة الأساسية للكمبيوتر والموبايل.
- عرض المنتجات والأقسام والأسعار والمصادر الأوروبية.
- إضافة وإزالة المنتجات من السلة داخل جلسة الصفحة.
- المفضلة داخل الجلسة.
- البحث المساعد والمقارنة والمنتجات المشاهدة مؤخرًا عبر localStorage.
- تبديل USD/SYP/EUR باستخدام معاملات ثابتة.
- فرز بعض قوائم المنتجات.
- خطوات checkout التجريبية.
- تسجيل دخول المدير التجريبي وحماية عدد من مسارات الإدارة من المستخدم غير المسجل.
- إضافة/حذف/تعديل Mock للأقسام والعروض داخل الحالة المحلية.
- تغيير حالة الطلب محليًا داخل صفحة التفاصيل.
- البناء والإقلاع على Next.js وPM2 وNginx بعد تجهيز السحابة.

### Mock-only أو غير مكتمل

- أي حفظ دائم للمنتجات أو الأقسام أو المخزون أو العروض.
- قاعدة البيانات وBackend وAPI.
- إنشاء الحسابات والمصادقة الآمنة والأدوار الحقيقية.
- الدفع الإلكتروني وإنشاء الطلبات الحقيقية.
- تطبيق الكوبونات على checkout.
- مزامنة لوحة الإدارة مع واجهة الزبون.
- تقارير مبنية على مبيعات فعلية.
- تصدير CSV/PDF حقيقي.
- رفع صور حقيقي وتخزين ملفات.
- إشعارات الطلبات والشحن والإرجاع.
- PWA إنتاجية مع Service Worker وHTTPS.

## 10. المشكلات والقيود المعروفة

1. **لا توجد قاعدة بيانات أو Backend:** أي تعديل يتم داخل الذاكرة أو localStorage يختفي كليًا أو جزئيًا بعد إعادة تحميل الصفحة.
2. **عدم وجود مصدر بيانات موحد:** توجد مصفوفات منتجات وطلبات متعددة في ملفات مختلفة، ولذلك قد لا تتطابق أرقام شاشة الزبون مع شاشة الإدارة.
3. **المسارات غير المكتملة:** صفحات /account/addresses و/account/returns وتفاصيل طلب الزبون ليست شاشات مستقلة، و/admin/roles و/admin/returns غير منفذة فعليًا.
4. **التصفية والتصدير:** بعض أزرار التصفية والتصدير في لوحة الإدارة واجهية فقط أو تنفذ رسالة تجريبية.
5. **السلة:** العناصر مخزنة كمصفوفة منتجات؛ زر الكمية يضيف أو يحذف نسخة من المنتج بدل وجود line item بكمية مستقلة.
6. **الدفع:** شاشة الدفع تنص صراحة على أنه تجريبي ولا تنفذ عملية مالية.
7. **الأمن:** بيانات مدير التجربة موجودة داخل JavaScript في المتصفح؛ لا يجوز استخدامها في بيئة تجارية حقيقية.
8. **الصور:** صور المنتجات تأتي من Unsplash عبر الإنترنت وليست ملفات منتجات محلية؛ لذلك تعتمد جودة العرض على الشبكة الخارجية.
9. **PWA:** لا يوجد Service Worker، وManifest يستخدم أيقونة SVG عامة فقط، ولا يوجد HTTPS أو دومين رسمي حاليًا.
10. **الوصول العام إلى 3005:** الخادم يستمع داخليًا والمنفذ مهيأ في PM2، لكن الوصول HTTP العام لم يثبت من بيئة التدقيق، بانتظار تفعيل/سماح مزود VPS.
11. **أداء الموبايل:** Canvas يستخدم 105 جسيمات تقريبًا على الشاشات الكبيرة و42 على الشاشات الصغيرة، وهو مناسب للعرض التجريبي لكنه يحتاج قياسًا على أجهزة قديمة. CSS يوقف الحركات عند prefers-reduced-motion.
12. **Safe area:** يوجد استخدام لـ 100dvh وenv(safe-area-inset-bottom) للشريط السفلي، لكن لا توجد منظومة كاملة لاختبار كل حالات Safari وفتحات الكاميرا والـ notch.
13. **الاستجابة:** توجد نقاط استجابة عند 1024 و700 بكسل، لكن لا يوجد اختبار آلي أو تقرير قياسات لكل جهاز Android وiPhone.

## 11. الخطوات المقترحة التالية

1. **إكمال مسار العرض التجاري أولًا:** تأكيد قبول صاحب مشروع Market والاتفاق التجاري قبل تحويل الواجهة التجريبية إلى نظام تشغيل فعلي.
2. **ربط الدومين الرسمي:** إضافة سجل DNS إلى عنوان VPS، ثم إعداد Nginx باسم الدومين وإصدار شهادة HTTPS.
3. **إكمال PWA:** إضافة أيقونات حقيقية بمقاسات 192 و256 و384 و512 وmaskable، إنشاء Service Worker وتسجيله، ثم اختبار التثبيت فعليًا على Android وSafari وDesktop عبر HTTPS.
4. **توحيد البيانات:** استخراج الأنواع والـ Mock data إلى مجلدات واضحة بدل تكرار المصفوفات داخل المكونات.
5. **المرحلة الكبرى التالية — Backend + Database:** بناء API ومصادقة وخدمة تخزين وربط قاعدة بيانات للمنتجات والأقسام والطلبات والعملاء والمخزون والعروض والتقارير. هذه المرحلة مستقبلية ولم تُنفذ في هذا التقرير.
6. **تحويل الحفظ التجريبي إلى عمليات حقيقية:** جعل إنشاء المنتج والقسم والعرض وتعديل المخزون وحالة الطلب عمليات محفوظة ومراجعة الصلاحيات على الخادم.
7. **إكمال التجارة الإلكترونية:** كوبونات حقيقية، مخزون فعلي، شحن، دفع، إشعارات، طلبات، مرتجعات، وفواتير.
8. **اختبار قبول العميل:** اختبار المسارات الأساسية على Chrome Android وSafari iPhone وDesktop، ثم توثيق المشاكل البصرية والأداء قبل تسليم النسخة التجارية.

---

## خلاصة الحالة

المشروع صالح حاليًا كـ **Demo Frontend مقنع** لعرض فكرة NOVA MARKET وتصميمها ومسار تجربة الزبون والمدير. البناء المحلي ناجح، ونسخة Next.js تعمل على VPS داخليًا خلف Nginx على 3005. لكنه ليس نظام متجر إنتاجيًا بعد: لا توجد قاعدة بيانات أو Backend أو مصادقة آمنة أو PWA مكتملة أو دومين HTTPS رسمي.
